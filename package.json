{
  "name": "glitcher-app",
  "description": "A dynamic SVG glitch effect generator with real-time preview and customization.",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vfx-js/core": "^0.6.0",
    "gradient-gl": "^1.2.0"
  },
  "devDependencies": {
    "vite": "6.*"
  }
}
